name: detectorrbt

channels:
  - nvidia
  - pytorch
  - conda-forge
  - defaults

dependencies:
  # =========================
  # Core
  # =========================
  - python=3.10
  - pip

  # =========================
  # Scientific stack
  # =========================
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pillow
  - pyyaml
  - tqdm
  - requests

  # =========================
  # PyTorch GPU
  # =========================
  - pytorch
  - torchvision
  - pytorch-cuda=12.4

  # =========================
  # NVIDIA / CUDA / TensorRT (Conda)
  # =========================
  - cuda-version=12.4
  - tensorrt=10.*

  # =========================
  # ONNX core (Conda)
  # =========================
  - onnx

  # =========================
  # NVIDIA utils
  # =========================
  - nvidia-ml-py
  - gpustat

  # =========================
  # Pip-only packages
  # =========================
  - pip:
      # YOLO
      - ultralytics>=8.0.0

      # ONNX Runtime GPU (pip-only)
      - onnxruntime-gpu>=1.15.1,<2.0.0

      # ONNX simplifier (pip-only)
      - onnxsim

      # OpenVINO
      - openvino-dev>=2023.0.0

      # ONNX optimizer
      - onnxslim>=0.1.71
